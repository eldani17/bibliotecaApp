{% extends 'base.html.twig' %}

{% block contenidoDerecho %}
  <div class="col-lg-9 col-md-8 col-sm-12">
    <div class="row separacionResponsiva">
      <div class="col-lg-12">
        <a class="btn btn-success" href="{{ path('idioma_crear') }}">Crear un nuevo idioma</a>
      </div>
    </div>
    <div class="row">
      <div class="col-lg-12">
        <h3>Lista de Idiomas</h3>

        <table class="table table-hover table-bordered">
          <thead class="table-dark">
            <tr>
              <th scope="">Nombre</th>
              <th scope=""></th>
            </tr>
          </thead>
            <tbody>
            {% for idioma in idiomas %}
              <tr scope="row">
                <td><a class="link-bilio" href="{{ path('idioma_mostrar', { 'id': idioma.id }) }}">{{ idioma.nombre }}</a></td>
                <td>
                  <a class="btn btn-warning" href="{{ path('idioma_editar', { 'id': idioma.id }) }}">Editar</a>
                  <a class="btn btn-danger" href="{{ path('idioma_borrar', { 'id': idioma.id }) }}">Borrar</a>
                </td>
              </tr>
            {% endfor %}
            </tbody>
        </table>
      </div>
    </div>
  </div>
  <!-- /.col-lg-9 -->
{% endblock %}
