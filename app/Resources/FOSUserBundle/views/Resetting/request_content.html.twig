{% trans_default_domain 'FOSUserBundle' %}

<div class="text-center">
  <form class="fos_user_resetting_request" action="{{ path("fos_user_resetting_send_email") }}" method="POST">
      <h1 class="h3 mb-3 font-weight-normal">Recuperar contraseña</h1>

      <label class="sr-only mb-3" for="username">{{ 'resetting.request.username'|trans }}</label>
      <input class="form-control" type="text" id="username" name="username"
      required="required" autocomplete="username" autofocus placeholder="{{ 'resetting.request.username'|trans }}"/>

      <button class="btn btn-lg btn-block btn-biblio" id="_submit" name="_submit">{{ 'resetting.request.submit'|trans }}</button>
  </form>
</div>
